/*input(num1, num2) if num2 is greater return true, if smaller return false
if nums are equal return -1 */

function CheckNums(num1,num2) {

return (num2 > num1) ? true : (num1 > num2) ? false : -1;

}
